ThisBuild / scalaVersion := "3.3.3"

ThisBuild / organization := "com.navrose.fp"

ThisBuild / version := "0.1.0"

lazy val root = (project in file("."))
  .settings(
    name := "functional-programming-in-scala"
  )

